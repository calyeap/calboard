# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies / build output (placeholder for M1 scaffolding)
node_modules/
.next/
dist/
*.tsbuildinfo

# Local dev tooling — not part of the app
.claude/launch.json

calboard-dashboard-mock-v7.html

.worktrees/
