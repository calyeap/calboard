# Screenshots

Referenced from the project README but not yet supplied. Expected files:

- `dashboard.png` — the main portfolio Dashboard
- `holdings-editor.png` — the Holdings editor

Drop the PNGs in this directory using those exact filenames and the README's
image links will resolve.
