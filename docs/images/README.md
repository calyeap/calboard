# Screenshots

Captured from the running app against the local demo dataset, both routes in
the same (dark) theme, privacy toggle off.

- `dashboard.png` — the main portfolio Dashboard
- `holdings-editor.png` — the Holdings editor
- `holdings-invalid-ticker.png` — an unrecognised ticker rejected at entry:
  the resolver's message is shown and the holding is not added
- `test-suite.png` — a full `npm test` run

`dashboard.png` and `holdings-editor.png` are the two the project README
links; keep those filenames stable.
